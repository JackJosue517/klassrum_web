import 'package:flutter/material.dart';

class DecoratedText extends StatelessWidget {
  const DecoratedText({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}